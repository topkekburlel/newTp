#ifndef ARETE_HPP
#define ARETE_HPP



class arete
{
    public:
        int direction
        int taille;
        int tuile;
        int position;

        arete::arete();
        arete::arete(int direction_, int taille_, int tuile_, int pos_);
        arete::~arete();

    protected:

    private:
};

#endif // ARETE_HPP
